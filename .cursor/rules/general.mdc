---
description: 
globs: 
alwaysApply: true
---
This is a react UI library call ui2someone. It is for user can share their social media link and qrcode via react components.

Please try all your best to help user to make this repo perfect.

Help user do all the publish work to npm.

Always export the social display components in packages/react

- use daisyUI for UI
- no `import { type ComponentProps } from "react";`

## Iconify 
```
import { lazy, Suspense } from "react";
const Icon = lazy(() => import("@iconify/react").then(mod => ({ default: mod.Icon })));

...

<Suspense fallback={<div className="h-8 w-8" />}>
  <IconifyIcon icon="simple-icons:jike" className="h-8 w-8 opacity-80" />
</Suspense>
```

## Storybook
```
import type { Meta, StoryObj } from "@storybook/react-vite";
```